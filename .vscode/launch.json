{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "(lldb) Launch",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/main.out",
      "args": [],
      "cwd": "${fileDirname}",
      "preLaunchTask": "Build with Clang"
    }
  ]
}
